<div class="post-content">
  <%= form_for(@blog) do |f| %>
    <h3>編集する</h3>
    <%= "投稿者:#{@blog.name} "%>
    <%= f.text_field :text, value: "#{@blog.text}" %>
    <%= f.submit "送信" %>
  <% end %>
</div>